
export interface REData_Attribute {
    /** ID (0 is Invalid). */
    id : number;

    /** Name. */
    name: string;
}

export interface REData_Behavior{
    /** ID (0 is Invalid). */
    id : number;

    /** Name. */
    name: string;
}

