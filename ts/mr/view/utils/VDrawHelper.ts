

export class VBitmapHelper {

    // public static drawCurrencyValue(window: Window_Base, value: string, unit: string, x: number, y: number, width: number) {
    //     const unitWidth = Math.min(80, window.textWidth(unit));
    //     window.resetTextColor();
    //     window.drawText(value, x, y, width - unitWidth - 6, "right");
    //     window.changeTextColor(ColorManager.systemColor());
    //     window.drawText(unit, x + width - unitWidth, y, unitWidth, "right");
    // }
    // Emphasized
}

